[package]
name = "savefile-abi"
version = "0.17.0-beta.1"
edition = "2021"

[features]
serde_derive = ["dep:serde_derive","serde"]

[dependencies]
savefile = { path="../savefile", version = "=0.17.0-beta.1"}
savefile-derive = { path="../savefile-derive", version = "=0.17.0-beta.1"}
byteorder = "1.4"
libloading = "0.8"
serde_derive = {version= "1.0", optional = true}
serde = {version= "1.0", optional = true}

[dev-dependencies]
serde="*"
serde_derive="*"
